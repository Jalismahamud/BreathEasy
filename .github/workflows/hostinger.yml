name: Deploy project to Hostinger server
on:
  push:
    branches:
      - dev
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.1.0
        with:
          fetch-depth: 2
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          # server: ftp.breatheasy222.site
          # username: u185768090.breatheasy
          # password: "cL0oU8WiqIfC8#Td"
          server: 72.60.163.17
          username: breatheasy222_backend
          password: "F@3csVnNtbNWdtFKYd%U"
